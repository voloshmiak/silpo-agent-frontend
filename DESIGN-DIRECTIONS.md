# SilpoFit — чотири візуальні напрямки

- **Порівняння всіх напрямків (код, живе):** https://claude.ai/code/artifact/1be60941-2afc-44bc-bfe0-3c0a2bee27bc
- **Figma (напрямки 01–02):** https://www.figma.com/design/M6t0Mp8ftHfVmtzzFvIG6F

Копія в макетах українською, усі шрифти з підтримкою кирилиці.
Контент однаковий у всіх напрямках — тиждень 12, бюджет 1 850 / 2 000 ₴,
реальні товари «Сільпо» з акціями, синхронізація через MCP. Різниця — лише дизайн.

## Спільний каркас

Сітка 1440, зовнішні поля 88 (у напрямку 04 — 40, бо картки мають власні відступи).
Стиль-тайл у Figma побудований як редакційний двоколонковий розворот: мітка секції
моно-шрифтом у колонці 180 px зліва, контент праворуч. Секції: ПАЛІТРА /
ТИПОГРАФІКА / КОМПОНЕНТИ / ПРИНЦИПИ.

Дашборд у кожному напрямку має **різну структуру**, не лише інші кольори —
це головне, за чим їх треба порівнювати.

---

## 01 · Папір та кислота  ✅ Figma + код

Тепле паперове тло, майже чорна фарба, широкі асиметричні поля,
один кислотний акцент. Гастрономічне видання, не фітнес-трекер.

| Токен | Hex | Роль |
|---|---|---|
| `color.paper` | `#F2EEE4` | тло сторінки |
| `color.paper.sunk` | `#E7E1D2` | втоплені блоки, мініатюри |
| `color.ink` | `#14150F` | основний текст |
| `color.clay` | `#7A7768` | другорядний текст |
| `color.acid` | `#C7F03C` | акцент, дії, тренувальний день |
| `color.terra` | `#B4472E` | перевитрата, стоп-продукти |
| `color.rule` | `#CFC8B6` | волосяні лінії |

Дисплей — Playfair Display Medium (−3 % трекінг). UI — Inter.
Цифри — IBM Plex Mono, вирівняні праворуч. Радіус максимум 2 px, тіней немає.

**Структура:** масштабний масthead зліва + статистика праворуч, раціон як
**редакційна таблиця** на 7 рядків, деталь дня під нею, кошик «Сільпо» —
вертикальний rail праворуч з волосяною лінією замість картки.

## 02 · Нічна кухня  ◐ Figma (стиль-тайл + частина дашборду) · ✅ код

Глибоке вугільно-оливкове тло, тепла кремова фарба, стиснутий капітальний
дисплей. Продукти світяться на темному.

| Токен | Hex | Роль |
|---|---|---|
| `color.night` | `#141613` | тло сторінки |
| `color.surface` | `#1D211B` | картки |
| `color.surface.raised` | `#272C24` | активна картка, мініатюри |
| `color.cream` | `#EDE9DE` | основний текст |
| `color.ash` | `#8C9184` | другорядний текст |
| `color.lime` | `#D9F55E` | акцент, дії |
| `color.amber` | `#E8A33D` | акції, знижки, увага |
| `color.hairline` | `#343A30` | межі карток |

Дисплей — Oswald Medium CAPS (+2 % трекінг). UI — Manrope. Дані — IBM Plex Mono.

**Структура:** тиждень читається як **сім карток у ряд**, а не таблиця.
Активний день — лаймова рамка. Нижче спліт: деталь дня зліва (780),
картка кошика + блок фідбеку справа (440).

**У Figma зроблено:** хедер, hero-банд зі статистикою, стрічка з 7 карток днів.
**У Figma лишилось:** нижній спліт — прийоми їжі, картка кошика, блок фідбеку.

## 03 · Моно + Сільпо  ○ тільки код

Брутальна редакційна сітка: чистий білий, чорна фарба, червоний «Сільпо»
як єдиний колір. Найсміливіший з авторських напрямків.

| Токен | Hex | Роль |
|---|---|---|
| `color.white` | `#FFFFFF` | тло |
| `color.ink` | `#0A0A0A` | текст, лінії |
| `color.silpo` | `#E4002B` | єдиний акцент |
| `color.grey` | `#6E6E6E` | другорядний текст |

Дисплей — **Inter Tight 900, −4 %**, великим кеглем, CAPS.
Решта — **суцільно IBM Plex Mono** (це і є «моно» в назві напрямку).
Жорсткий видимий 1 px грід, нуль радіусів, нуль тіней.

**Структура:** газетна сітка з видимими лініями колонок; раціон — щільний
7-денний **календарний грід**; активний день інвертований у чорне;
кошик — **повноширинна нижня смуга** з червоним блоком підсумку.

## 04 · AI Factory  ○ тільки код

Мова материнського бренду, знята зі справжніх токенів
[ai-factory.silpo.ua](https://ai-factory.silpo.ua/). Найбезпечніший вибір,
якщо агента показують журі «Сільпо»: читається як частина їхньої екосистеми.
Ціна — найменша власна індивідуальність із чотирьох.

Сайт зберігає палітру в `oklch` (Tailwind v4); нижче — переведено в hex.

| Токен сайту | Hex | Роль |
|---|---|---|
| `--brand-cream` / `--background` | `#FFF7EA` | тло сторінки |
| `--secondary` | `#FBECD9` | теплі плашки, підсвітка |
| `--muted` | `#F9F1E3` | нейтральні плашки, мініатюри |
| `--card` | `#FFFFFF` | картки |
| `--brand-ink` / `--foreground` | `#221812` | основний текст (теплий, не чорний) |
| `--muted-foreground` | `#6B5A4D` | другорядний текст |
| `--border` / `--input` | `#E6DCD0` | межі карток |
| `--brand-orange` / `--primary` | `#FE860F` | основна дія |
| `--brand-yellow` / `--accent` | `#FFD939` | підсвітка, акцентна картка |
| `--brand-blue` | `#317DE9` | другорядна мітка (кардіо) |
| `--destructive` | `#E60016` | акції, знижки |

**Тіні теплі, а не сірі** — це головна деталь, яку легко проґавити:

```css
--shadow-soft: 0 10px 30px -12px rgba(103, 60, 20, .18);  /* oklch(40% .08 60/.18) */
--shadow-pop:  0 20px 50px -18px rgba(188, 73, 0, .35);   /* oklch(55% .18 55/.35) */
```

`--radius: 1rem` (16 px), кнопки й теги — пігулки (999 px).
Волосяних ліній немає взагалі: ієрархію тримають картки й повітря.

**Шрифт.** Сайт використовує пропрієтарний **Silpo Text** (400/500/600/700/900)
з `/fonts/SilpoText-*.woff2`. У макеті-порівнянні стоїть **Onest** як підмінник —
геометричний гуманістичний гротеск із повною кирилицею й тим самим діапазоном ваг.
Якщо обираємо цей напрямок, у продакшені треба покласти справжні `.woff2`
у `public/fonts/` і оголосити `@font-face` — тоді збіг буде точний.

**Структура:** плаваюча пігулка-навігація; hero з текстом зліва й карткою бюджету
справа; ряд із 4 stat-карток (одна — жовта акцентна); тиждень як 7 заокруглених
карток; спліт «прийоми їжі / кошик»; жовта смуга фідбеку на всю ширину.

---

## Що лишилось зробити у Figma

Ліміт MCP на Starter-плані — 20 викликів на місяць, вичерпано.
Коли відновиться (або після апгрейду плану), один прохід:

1. `02` — нижній спліт дашборду (прийоми їжі, кошик, фідбек) — ~2 виклики
2. `03` — стиль-тайл + дашборд — ~6 викликів
3. `04` — стиль-тайл + дашборд — ~6 викликів
4. Фінальні скріншоти для звірки — ~2 виклики

Starter-план також обмежує файл трьома сторінками, тож для напрямків 03–04
знадобиться або окремий Figma-файл, або апгрейд.
